// How long a surface stays mounted after it is dismissed, so its exit
// animation can finish. These mirror the matching CSS animation durations in
// `src/styles/global.css` — keep the two in step.
export const DIALOG_EXIT_MS = 180
export const MENU_EXIT_MS = 120
export const ARCHIVE_EXIT_MS = 180
